ALTER TABLE mangas
ADD COLUMN last_modified_at INTEGER AS Long DEFAULT (strftime('%s', 'now'));

CREATE TRIGGER update_last_modified_at
AFTER UPDATE ON mangas
FOR EACH ROW
BEGIN
  UPDATE mangas
  SET last_modified_at = strftime('%s', 'now')
  WHERE _id = NEW._id;
END;